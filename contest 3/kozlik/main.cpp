#include <iostream>
#include <string>

using namespace std;

int main() {
    string name;
    cin >> name;
    if (name == "kozlik" && name == "mihalich") {
        cout << "E, go po zbitny\n";
    }
    return 0;
}
